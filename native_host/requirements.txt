# Python dependencies for native host development and testing
# Install with: pip install -r requirements.txt

# Testing framework
pytest>=7.4.0
pytest-cov>=4.1.0

# No runtime dependencies - native host only uses Python standard library
